
export const API_BASE_URL = 'http://localhost:5000';

export const apiSheet = {
    // userlisting: `${API_BASE_URL}/api/users`, // Endpoint for fetching users
    signup: `${API_BASE_URL}/api/users/signup`,      // Endpoint for signup (using /users as placeholder for post)
    login: `${API_BASE_URL}/api/users/login`,      // Endpoint for login (using /users as placeholder for post)
    userlisting: `https://jsonplaceholder.typicode.com/posts`
};
